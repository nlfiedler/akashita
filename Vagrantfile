#
# Vagrantfile for Ubuntu 18.04
#
Vagrant.configure(2) do |config|
  config.vm.box = 'ubuntu/bionic64'
  config.vm.hostname = 'deamon'
  config.vm.provider 'virtualbox' do |vb|
    vb.memory = 4096
    vb.cpus = 2
  end
end
